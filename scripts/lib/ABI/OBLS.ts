import { parseAbi } from "viem";

export const OBLS_ABI = parseAbi([
  ////////////////////////////////////////
  //////////// READ functions ////////////

  "function votingPower(uint256 index) external view returns (uint256)",
  "function isActive(uint256 operatorIndex) external view returns (bool)",
  ////////////////////////////////////////
  ////////////////////////////////////////

  ////////////////////////////////////////
  /////////// WRITE functions ////////////
  "function increaseOperatorVotingPower(uint256 index, uint256 votingPower) external"
  ////////////////////////////////////////
  ////////////////////////////////////////
] as const);
